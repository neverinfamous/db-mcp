# .npmrc — db-mcp supply chain hardening
#
# Disable automatic execution of install scripts from transitive
# dependencies to prevent arbitrary code execution during npm install.
# better-sqlite3 requires native compilation via install scripts,
# so it must be explicitly allowed.
#
# See: https://docs.npmjs.com/cli/v10/using-npm/scripts#best-practices

ignore-scripts=true
